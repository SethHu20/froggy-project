#include "board.hpp"
#include "engine.hpp"
#include <iostream>


int main() {
  Board board;
  Engine engine;

  std::string input;
  while (true) {
    std::cin >> input;
    // if (input == "uci")
    // else if (input == "debug")
    // else if (input)
  }
}